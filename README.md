# SEMESTER-1

`pip install numpy`

`pip install pandas`

`pip install matplotlib`


2.Nested Structure:

3.Program for Writing to and reading from a file

4.Program to write characters A to Z into a file and read the file and print the characters in lowercase.